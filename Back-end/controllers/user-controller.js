const user_model = require("../models/admin-model");
const user_controller = {};
user_controller.add_user = async (req, res, next) => {
  const { name, contact, email, username, password, role } = req.body;
  user;
};
module.exports = user_controller;
